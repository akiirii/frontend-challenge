{
  "name": "frontend-challenge",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:fliit/frontend-challenge.git",
  "license": "UNLICENSED",
  "scripts": {
    "setup": "yarn && lerna bootstrap --npm-client=yarn",
    "start": "lerna run --parallel serve --npm-client=yarn"
  },
  "devDependencies": {
    "lerna": "^3.4.0"
  }
}
